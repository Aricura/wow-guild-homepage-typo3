<section class="content-carousel js-slider" id="c{{ element.getKey }}">
    {# slides #}
    <div class="content-carousel-slides js-slider-slides">
        {% for slide in element.getContentCarouselSlides %}
            {{ include('content-carousel/item.html.twig', {
                'slide': slide,
            }) }}
        {% endfor %}
    </div>
    {# overlay #}
    <div class="content-carousel-overlay">
        {# overlay template placeholder #}
        <div class="content-carousel-overlay-container js-slider-overlay-container"></div>

        {% if element.getContentCarouselSlides|length > 1 %}
            {# controls #}
            <div class="slider-controls">
                <a class="slider-controls--prev js-slider-prev" href="javascript:void(0);"></a>
                <a class="slider-controls--next js-slider-next" href="javascript:void(0);"></a>
            </div>
        {% endif %}
    </div>
</section>
